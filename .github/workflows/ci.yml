name: CI

on:
  push:
    branches: [main, claude]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# PRIVACY: the repository is public. Never add `actions/upload-artifact` (or any upload/cache of
# test outputs, reports, traces or screenshots) to this workflow. Only synthetic fixtures exist in git.

jobs:
  privacy-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - run: python scripts/check_private_data.py

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: viper
          POSTGRES_PASSWORD: viper
          POSTGRES_DB: viper_test
        ports:
          - 5442:5432
        options: >-
          --health-cmd "pg_isready -U viper -d viper_test"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    env:
      VIPER_TEST_DATABASE_URL: postgresql+psycopg://viper:viper@127.0.0.1:5442/viper_test
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: backend/requirements*.txt
      - run: pip install -r requirements-dev.txt
      - run: ruff check . ../scripts
      - run: ruff format --check . ../scripts
      - run: mypy
      - name: Migration CLI smoke (upgrade / downgrade / upgrade)
        run: |
          alembic -x db=test upgrade head
          alembic -x db=test downgrade base
          alembic -x db=test upgrade head
      - run: pytest

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build

  e2e:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      # Shell smoke only (no backend): the API status indicator is not asserted.
      - run: npm run e2e
