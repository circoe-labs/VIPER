export const schema = `
PRAGMA foreign_keys=ON;
CREATE TABLE IF NOT EXISTS roles(id TEXT PRIMARY KEY,label TEXT NOT NULL UNIQUE,slug TEXT NOT NULL UNIQUE,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS commercial_segments(id TEXT PRIMARY KEY,label TEXT NOT NULL UNIQUE,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS activity_categories(id TEXT PRIMARY KEY,label TEXT NOT NULL UNIQUE,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS internal_referents(id TEXT PRIMARY KEY,first_name TEXT NOT NULL,last_name TEXT NOT NULL,email TEXT,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS companies(id TEXT PRIMARY KEY,display_name TEXT NOT NULL,legal_name TEXT,siren TEXT UNIQUE,website_url TEXT,email_domain TEXT,size_label TEXT,commercial_segment_id TEXT,project_done_with_circoe TEXT,project_type TEXT,circoe_references TEXT,client_approach TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(commercial_segment_id) REFERENCES commercial_segments(id));
CREATE TABLE IF NOT EXISTS company_activity_categories(company_id TEXT NOT NULL,category_id TEXT NOT NULL,PRIMARY KEY(company_id,category_id),FOREIGN KEY(company_id) REFERENCES companies(id) ON DELETE CASCADE,FOREIGN KEY(category_id) REFERENCES activity_categories(id));
CREATE TABLE IF NOT EXISTS establishments(id TEXT PRIMARY KEY,company_id TEXT NOT NULL,name TEXT,siret TEXT UNIQUE,line1 TEXT,line2 TEXT,postal_code TEXT,city TEXT,country TEXT,kind TEXT,is_primary INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id) REFERENCES companies(id) ON DELETE CASCADE);
CREATE TABLE IF NOT EXISTS prospects(id TEXT PRIMARY KEY,company_id TEXT NOT NULL,civility TEXT,first_name TEXT NOT NULL,last_name TEXT NOT NULL,role_id TEXT,exact_job_title TEXT,activity_status TEXT NOT NULL DEFAULT 'unknown',employment_verified_at TEXT,contactability_status TEXT NOT NULL DEFAULT 'contactable',do_not_contact_at TEXT,do_not_contact_reason TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id) REFERENCES companies(id),FOREIGN KEY(role_id) REFERENCES roles(id));
CREATE TABLE IF NOT EXISTS emails(id TEXT PRIMARY KEY,prospect_id TEXT NOT NULL,address TEXT NOT NULL,is_primary INTEGER NOT NULL DEFAULT 0,is_active INTEGER NOT NULL DEFAULT 1,verification_status TEXT NOT NULL DEFAULT 'unverified',origin_type TEXT NOT NULL DEFAULT 'manual',last_verified_at TEXT,source_reference TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(prospect_id) REFERENCES prospects(id) ON DELETE CASCADE);
CREATE UNIQUE INDEX IF NOT EXISTS one_primary_email ON emails(prospect_id) WHERE is_primary=1 AND is_active=1;
CREATE UNIQUE INDEX IF NOT EXISTS normalized_email_unique ON emails(lower(address));
CREATE TABLE IF NOT EXISTS phones(id TEXT PRIMARY KEY,prospect_id TEXT NOT NULL,number TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'other',is_primary INTEGER NOT NULL DEFAULT 0,is_active INTEGER NOT NULL DEFAULT 1,verification_status TEXT NOT NULL DEFAULT 'unverified',origin_type TEXT NOT NULL DEFAULT 'manual',last_verified_at TEXT,source_reference TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(prospect_id) REFERENCES prospects(id) ON DELETE CASCADE);
CREATE UNIQUE INDEX IF NOT EXISTS one_primary_phone ON phones(prospect_id) WHERE is_primary=1 AND is_active=1;
CREATE TABLE IF NOT EXISTS contact_tracking(id TEXT PRIMARY KEY,prospect_id TEXT NOT NULL UNIQUE,planned_contact_at TEXT,status TEXT NOT NULL DEFAULT 'to_contact',referent_id TEXT,response_received_at TEXT,appointment_at TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(prospect_id) REFERENCES prospects(id) ON DELETE CASCADE,FOREIGN KEY(referent_id) REFERENCES internal_referents(id));
CREATE TABLE IF NOT EXISTS contact_tracking_status_history(id TEXT PRIMARY KEY,contact_tracking_id TEXT NOT NULL,from_status TEXT,to_status TEXT NOT NULL,changed_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,actor_type TEXT NOT NULL,actor_id TEXT,FOREIGN KEY(contact_tracking_id) REFERENCES contact_tracking(id) ON DELETE CASCADE);
CREATE TABLE IF NOT EXISTS prospect_sources(id TEXT PRIMARY KEY,prospect_id TEXT NOT NULL,source_type TEXT NOT NULL,source_reference TEXT,collected_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,legal_basis_or_collection_context TEXT,created_by_actor TEXT,notes TEXT,FOREIGN KEY(prospect_id) REFERENCES prospects(id) ON DELETE CASCADE);
CREATE TABLE IF NOT EXISTS import_batches(id TEXT PRIMARY KEY,filename TEXT NOT NULL,sheets TEXT,imported_at TEXT,status TEXT NOT NULL,row_count INTEGER NOT NULL DEFAULT 0,accepted_count INTEGER NOT NULL DEFAULT 0,rejected_count INTEGER NOT NULL DEFAULT 0,actor_id TEXT,file_fingerprint TEXT);
CREATE TABLE IF NOT EXISTS import_row_metadata(id TEXT PRIMARY KEY,batch_id TEXT NOT NULL,source_sheet TEXT,source_row_number INTEGER,prospect_id TEXT,company_id TEXT,legacy_metadata TEXT NOT NULL DEFAULT '{}',FOREIGN KEY(batch_id) REFERENCES import_batches(id) ON DELETE CASCADE);
CREATE TABLE IF NOT EXISTS audit_log(id TEXT PRIMARY KEY,actor_type TEXT NOT NULL,actor_id TEXT,actor_display TEXT,entity_type TEXT NOT NULL,entity_id TEXT NOT NULL,action TEXT NOT NULL,changed_fields TEXT,before_payload TEXT,after_payload TEXT,source_context TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
`;