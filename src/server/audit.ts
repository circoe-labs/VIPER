import { randomUUID } from 'node:crypto'; import { db } from './db.js';
export type Actor={type:'human'|'import'|'agent'|'system';id?:string;display?:string};
export function audit(actor:Actor,entityType:string,entityId:string,action:string,before:unknown,after:unknown,sourceContext?:string){db.prepare(`INSERT INTO audit_log(id,actor_type,actor_id,actor_display,entity_type,entity_id,action,before_payload,after_payload,source_context) VALUES(?,?,?,?,?,?,?,?,?,?)`).run(randomUUID(),actor.type,actor.id??null,actor.display??null,entityType,entityId,action,JSON.stringify(before??null),JSON.stringify(after??null),sourceContext??null)}
