# Decision Log

> Living copy of the handoff decision log. Sections *Locked by the grill session* and *Requirements retained* are
> the human decisions from the handoff and must not be changed without explicit product approval. New decisions are
> appended under *Implementation decisions* with date, owner and rationale.

## Locked by the grill session

| # | Decision | Notes |
|---|---|---|
| 1 | V1 prioritizes database + interface + Excel/CSV round-trip | Automated prospecting/exploitation is explicitly postponed. |
| 2 | Excel is an import/export contract, not the internal schema | Normalize internally; do not reproduce legacy semantic mistakes. |
| 3 | Prospect = individual person; Company = employer/context | A company may have multiple prospects. |
| 4 | Referent and verification are separate concepts | `Référent` must only contain an internal Circoe person. |
| 5 | Prospect activity = `Active / Inactive / Unknown` | Separate from whether the information was verified. |
| 6 | Verification uses a nullable date | No date means never verified. |
| 7 | Company/employment verification and contact-channel verification are separate | Avoid one overloaded verification flag. |
| 8 | Emails use a separate alias table | Multiple emails, one active primary, own verification/source metadata. |
| 9 | Phones use the same separate-record pattern | Type, primary flag, verification/source metadata. |
|10 | `Fonction` becomes UI label `Rôle` | Role is an administrable taxonomy, with exact job title preserved separately. |
|11 | Unknown roles are suggested, not silently created on import | User can map/create/leave unclassified. |
|12 | Company supports one commercial segment and multiple activity categories | Both are distinct dimensions; taxonomies are administrable. |
|13 | SIREN/SIRET are supported from V1 and optional | Stable business identifiers when available. |
|14 | Company and Establishment are separate | Prospect is not directly linked to an establishment in V1. |
|15 | Legacy week codes become a real planned-contact date | Week number is derived for display; ambiguous year must not be invented. |
|16 | Legacy RDV/devis/suivi/relance booleans collapse to lightweight contact-tracking state | Preserve transition history; no rich CRM dossier. |
|17 | Technical object is `prospection`/contact tracking; UI wording is `Suivi de contact` | It exists before an appointment exists. |
|18 | Referent is nullable and relevant once a meeting is obtained/taken over | Future Calendly may set it automatically; V1 is manual. |
|19 | Home is a global dashboard, not merely an inbox | It still surfaces next actions. |
|20 | Prospection is a readable people list, not a dense spreadsheet | Counters act as filters; prospect opens in a large editor drawer/modal. |
|21 | Database is a genuine advanced data explorer | DBeaver-like inspection/editing is a core requirement. |
|22 | Exploitation route exists as Coming soon only | No fake IContact features. |
|23 | UI must give strong visual feedback for imported/unverified/stale dynamic information | Prefilled fields and minimal-effort manual verification are central. |
|24 | Same form component handles add/edit; Save & Next is desired | Preserve active queue/filter context. |
|25 | DA = Neon Command | Modern, dark-first, spacious, clean, neon accents used selectively. |
|26 | Selected VIPER identity = geometric V/viper mark | Keep all accepted black/white/neon mark/lockup variants for contrast/theme use. |

## Requirements retained from the source-of-truth for VIPER V1 foundation

These were not contradicted by the grill and must be preserved in the implementation foundation:

- controlled CSV/XLSX import with preview, incomplete-row handling, correction/exclusion and dedup;
- source/date and legal-basis or collection-context provenance for contacts;
- durable do-not-contact/opposition state, distinct from non-interest;
- audit of imports and meaningful mutations;
- one securely authenticated commercial user for the pilot, without overbuilding RBAC;
- a shared-data contract able to serve future IProspect/IContact;
- Home/reporting can show monthly contacted/appointment progress from manual data, but must not fake future agent/email integrations.

## Deferred / unresolved

- ~~exact frontend/backend/database stack~~ — resolved by I-01, see [ADR-0001](../adr/0001-stack.md);
- exact hosting, backup policy and retention/anonymization duration;
- ~~exact final export column order~~ — V1 default recorded in I-84, editable in one module (ADR-0013);
- exact semantics of legacy `Mode de contact` values (`Auto`, `Commercial`, `Commerciale`); preserve raw until mapped;
- conversion of legacy `S37/S39` without a known year;
- multiple independent historical contact cycles per prospect; V1 defaults to one current tracking record + transition history;
- pixel-perfect light-theme art direction; derive it conservatively from Neon Command;
- future mail/Calendly/agent contracts beyond the extension points needed by V1.

## Implementation decisions

| ID | Date | Owner | Decision | Rationale |
|---|---|---|---|---|
| I-01 | 2026-09-10 | Orchestrator (Task 00) | Stack follows existing Circoe conventions (IGuard, Onduline): **FastAPI + SQLAlchemy 2 + Alembic + PostgreSQL 16** backend, **React + TypeScript + Vite** frontend, pytest / Vitest / Playwright. Detailed in ADR-0001. | Handoff: "prefer existing Circoe conventions"; Postgres also gives a database-enforced read-only role for the SQL console (Task 13). |
| I-02 | 2026-09-10 | Orchestrator (Task 00) | The handoff `sources/` folder is git-ignored as a whole; spreadsheets are ignored by default except synthetic fixture folders. The rest of the handoff (`tasks/…`) is committed as a frozen reference. | Repo is public; handoff says never copy `sources/` wholesale into the repo. |
| I-03 | 2026-09-10 | Orchestrator (Task 00) | `doc/` is the living documentation; the handoff folder is a frozen snapshot except `tasks/TODO.md` statuses and per-task implementation reports. | User requirement: technical decisions and human intentions must stay durable for the whole project. |
| I-04 | 2026-09-10 | Orchestrator (Task 00) | User-facing UI copy is **French**; code, identifiers and technical docs are English. | The operator is French-speaking; the spec already uses French UI terms (`Suivi de contact`, `Rôle`, `Enregistrer et suivant`). |
| I-05 | 2026-09-10 | Orchestrator (Task 00) | The `/caveman` and `/coding-guideline` skills required by the handoff are not installed on this machine; `doc/process/agent-brief.md` provides the equivalent coding rules. | Unblock implementation while keeping explicit guidelines. |
| I-06 | 2026-09-10 | Orchestrator (Task 00) | Local Postgres for VIPER is exposed on host port **5442**. | Port 5432 is already used by another local project. |
| I-07 | 2026-09-10 | Orchestrator (Task 00) | Work happens on local branch `claude`; nothing is pushed to the public remote without explicit user approval. | Outward-facing action on a public repo. |
| I-08 | 2026-09-10 | Task 01 | Frontend URL paths use English segments (`/`, `/prospection`, `/exploitation`, `/database`, `/settings`); only visible labels are French (`Accueil`, `Base de données`, `Paramètres`). Backend dev port **8042**, Vite **5173**. | Paths are identifiers (I-04: code in English); ports 8000/5432 are used by other local projects. |
| I-09 | 2026-09-10 | Task 03 | Schema conventions per [ADR-0002](../adr/0002-data-schema-conventions.md): UUID (v7) keys, `timestamptz` with trigger-maintained `updated_at`, fixed value sets as `varchar(32)` + CHECK (not native enums), deterministic constraint names, explicit ON DELETE rules, normalized storage for SIREN/SIRET/emails/domains/phones. | Stable ids for future agents/shared DB; enums that evolve with a one-line migration; integrity enforced whatever the write path (import, Database Explorer, agents). |
| I-10 | 2026-09-10 | Task 03 | `prospects.first_name` and `last_name` are each nullable, with a CHECK requiring at least one non-blank name. `prospects.company_id` is nullable in the database ("normally required" is enforced by services). | Legacy rows may carry only one name part or no resolvable company; a prospect is still a person, so a fully anonymous row is rejected. |
| I-11 | 2026-09-10 | Task 03 | Civility is normalized to `mr` / `ms` (UI `M.` / `Mme`); legacy variants map through explicit import rules, anything else is flagged. | Data model says "nullable/normalized" without values; two codes cover the observed variants. |
| I-12 | 2026-09-10 | Task 03 | Do-not-contact durability is enforced by a database trigger: a `do_not_contact` prospect can only be reset by `clear_do_not_contact` (mandatory reason) and **cannot be deleted** (clear first, then delete). The clearing reason is persisted by the audit event once Task 05 exists (validated but not stored before that). | "Import/merge/new tracking/Database Explorer must not silently reactivate" (security spec, overview); deleting would erase the opposition and let a re-import recreate the person as contactable. Final retention/erasure policy stays open (open question #2). |
| I-13 | 2026-09-10 | Task 03 | Company-change rule: `employment_verified_at` is **cleared** (not superseded); every **active** email/phone is treated as company-dependent in V1 and moves `verified` → `unverified`, keeping `last_verified_at`; `invalid`/`unknown`/inactive channels are untouched; nothing is deleted. Applies to any change of `company_id`. | Overview leaves "clear or supersede" to implementation; NULL already means "current employment context not verified". No reliable signal distinguishes personal from professional channels in a B2B base; re-verifying one extra channel is cheap, missing one is not. |
| I-14 | 2026-09-10 | Task 03 | Email addresses and phone numbers are unique **per prospect**, not globally. | The legacy workbook has duplicate emails across rows; whether they are one person is a dedup decision (Tasks 08/09), not a storage error. |
| I-15 | 2026-09-10 | Task 03 | Provenance: `prospect_sources.collected_at` is NOT NULL (default now = when VIPER obtained the data); `import_batch_id` FK added for structured import provenance; `created_by_actor` is an actor snapshot (`actor_type`, `actor_id`, `actor_display`), as on `contact_tracking_status_history`, `import_batches` and `audit_log`. | The true original collection date of legacy rows is unknown; the import date is known. Actor kinds (human/import/system/agent) share no table, and labels must not change retroactively. |
| I-16 | 2026-09-10 | Task 03 | Import batches: `status` `pending/committed/failed/cancelled`, `committed_at` (required iff committed), `rows_total/rows_imported/rows_skipped`, `sheet_names text[]`, optional SHA-256 `file_fingerprint`; no workbook bytes. `import_row_metadata` is write-once (`created_at` only) and unique per batch/sheet/row. `ActorContext` placeholder lives in `app/core/actor.py` for Task 04 to populate. | Data model lists "row counts, status" without values; Task 09 may refine the status set with a CHECK migration. |
| I-17 | 2026-09-10 | Orchestrator review of Task 03 | **Services flush, callers commit.** Services/repositories never `commit()`/`rollback()`; `unit_of_work(session_factory)` (`app/db/session.py`) owns the transaction, used by `SessionDep` (one request = one transaction, `Depends(scope="function")` so the commit precedes the response) and by CLI/jobs. Amends ADR-0001's "services call `session.commit()`". | Prospect editor save (Task 15), import commit (Task 09) and staged Database Explorer changes (Task 12) must compose several service calls atomically. |
| I-20 | 2026-09-10 | Task 02 | The in-app theme defaults to **dark** and only changes on an explicit user choice (persisted in `localStorage`); the OS `prefers-color-scheme` is not followed for the app, only for the favicon (white mark on dark browser chrome, black mark on light). Styling approach in ADR-0003. | Dark is the authored Neon Command identity and light a conservative derivation (locked #25, deferred "pixel-perfect light theme"); a predictable default avoids users landing on the secondary theme by accident. |
| I-21 | 2026-09-10 | Task 02 | The app ships web-sized derivatives of the six accepted logos (cropped to the artwork, 2x downsampled, alpha kept, C2PA metadata not carried over) generated by `frontend/scripts/optimize-brand-assets.js`; originals stay untouched in the handoff folder. Logo artwork is used as delivered: its neon is a lime (~`#79FA03`) that differs from the UI accent Viper Green `#00E676`. | 1.6 MB → ~420 KB without visible loss; no new logo direction (Task 02 out of scope). Whether the logo neon and the UI green should converge is a product/brand question, not an implementation one. |
| I-22 | 2026-09-10 | Task 02 | "Success/verified" status uses a soft mint (`--color-success-fg`), distinct from the brand Viper Green, and every status badge carries a glyph + text. A development-only component showcase is served at `/_dev/ui` by the Vite dev server and stripped from production builds. | Design system: brand green must not be the universal success colour and status must never be colour-only; the showcase gives later UI tasks and reviewers a live catalogue without shipping it. |
| I-23 | 2026-09-10 | Task 04 | Authentication per [ADR-0004](../adr/0004-authentication-sessions.md): server-side sessions in PostgreSQL (`users`, `user_sessions`, only the SHA-256 of the token stored; idle 120 min / absolute 12 h, configurable), argon2id (RFC 9106 low-memory profile), cookie `viper_session` HttpOnly + Secure (default) + **SameSite=Strict** + `Path=/api`, session-bound CSRF token in `X-CSRF-Token` on unsafe methods, in-process sign-in throttling (5 failures per e-mail / 20 per client address per 15 min). Every `/api/*` route is protected by the `api_router` dependency except the allowlist `GET /api/health`, `POST /api/auth/login` (+ OpenAPI schema/docs). | The handoff leaves the mechanism open ("appropriate to chosen stack"); a single same-origin SPA needs revocable sessions, not JWT. Strict instead of Lax costs nothing because the cookie is API-only. In-process throttling is enough for the single-instance pilot (limits documented in the ADR). |
| I-24 | 2026-09-10 | Task 04 | Account management is CLI-only in V1: `python -m app.cli create-user` creates the account or resets its password (revoking its sessions); no sign-up, no in-app password change, no roles, no default account. `users` allows several rows but the pilot has one. The OpenAPI schema and `/api/docs` stay reachable without a session. | Out of scope per task ("no RBAC matrix"); a CLI keeps passwords out of arguments, env files and the repo. The API schema describes public code and carries no data. |
| I-25 | 2026-09-10 | Task 04 | Playwright now runs **full stack** (real backend on a dedicated `viper_e2e` database, ports 8044/5180, locally and in CI with a PostgreSQL service); global setup migrates the database and creates a synthetic account through the CLI with a random per-run password. The shell/design specs sign in through the API first. The backend `client` test fixture is signed in by default (`anonymous_client` for unauthenticated cases). Amends ADR-0001 ("CI e2e = shell smoke, no backend"). | Auth flows (sign-in, reload, sign-out revocation, deep link, cookie flags) cannot be tested without the backend; dedicated ports and database keep E2E away from the dev servers and data. A signed-in default client lets later feature routers (e.g. Task 11) be tested without auth plumbing. |
| I-26 | 2026-09-10 | Task 05 | Audit integration per [ADR-0006](../adr/0006-audit-integration.md): services `annotate` the rows they change (who, semantic action, reason, labels) and one `after_flush` hook writes exactly one event per changed audited row in the same transaction; rows nobody annotated get `<entity>.created/updated/deleted` with the actor bound to the session. `require_session` binds the signed-in user (`source=ui`, `request_id`) to every protected request; routers can switch the source (`audit_source(DATABASE_EXPLORER)`); imports and CLI bind their own actor. `audit_log` gains `subject_type`/`subject_id` (migration 0004) so a prospect's history includes its child rows. The action vocabulary is closed (lifecycle actions of registered entities + a short `AuditAction` list). The taxonomy seed and the `create-user` CLI are audited with `system` actors. | Forms, import and generic Database Explorer edits must all be audited with a server-side actor, without double logging and without every later agent hand-writing diffs. Explicit-only calls are forgettable; a listener alone has no meaning; both side by side need coverage bookkeeping. |
| I-27 | 2026-09-10 | Task 05 | Contact personal data in audit `changes` (prospect names, email addresses, phone numbers, source references) is stored **in full** in V1, behind one switch `app.core.audit_policy.POLICY.personal_values` (`full` → `masked` → `omitted`). Secrets are never stored (account/session entities carry no field values; password/token/CSRF-like fields dropped at any depth); `legacy_metadata` is always masked; payloads never go to application logs. | The history must say "email changed from X to Y" (Task 19) and the company-change rule requires old values in history; the single operator already sees these values through the same authenticated API, and the log is neither exported nor logged. Audit is append-only and retention is undecided (open question #2): the switch tightens new events without a schema change; existing events would need a subject-scoped redaction migration, which `subject_id` makes targetable. |
| I-28 | 2026-09-10 | Task 05 | The do-not-contact **clearing reason** is persisted in the `prospect.do_not_contact.cleared` audit event (`context.reason`), closing the gap noted in I-12; setting a restriction records its reason both on the prospect row and in `context.reason`. Reasons are never masked. | The prospect row can only describe an active restriction (CHECK: contactable ⇒ no reason), so the audit event is the durable record of why an opposition was lifted. |
| I-29 | 2026-09-10 | Task 05 | Import actor convention: the batch row keeps the **human** who started it and batch lifecycle events (`import_batch.started/committed/failed/cancelled`) are by that human; rows written by the import run inside `import_batches.importing(...)` as `ActorContext(IMPORT, id=<batch id>, display="Import <filename>")` with context `source=import`, `import_batch_id` and `on_behalf_of=<human>`. `import_row_metadata` rows are **not** audited individually (the entity events and batch counts cover the import; raw legacy values must not be copied into the undeletable log); `import_row_metadata` is not in the audited registry, so the Database Explorer must keep it read-only. Import provenance references read `<file> / <sheet> / ligne <n>`. | Import writes must be distinguishable from manual ones (Task 19) yet traceable to the person who confirmed them; per-row audit events would duplicate provenance and legacy personal data in a log that erasure cannot reach. |
| I-30 | 2026-09-10 | Task 05 | Security trail: `auth.login`, `auth.logout`, `auth.user_created`, `auth.password_reset` record who and when only — no token, token hash, CSRF token, session id, IP address or user agent. Failed sign-ins are not audited. Account creation/reset from the CLI is attributed to `ActorContext(SYSTEM, "Ligne de commande", id="app.cli")`; `create_or_reset_user` now takes the actor as its second argument. | IPs/user agents are personal data with no operator use in a one-account pilot (abuse is handled by the in-memory throttle); a failed attempt has no authenticated actor and its typed identifier could be a mistyped password. |
| I-31 | 2026-09-10 | Orchestrator review of Task 05 | Amends I-26: the audit hook **fails closed**. A flush that changes a row of an audited table with neither an annotation nor a bound actor raises `UnattributedMutationError` and the transaction rolls back (previously: silently not audited). Every table is classified — `AUDITED_ENTITIES` or `NOT_AUDITED_TABLES` with a reason — and a test enforces it. Non-HTTP code opens `audit.attributed_unit_of_work(session_factory, actor)` (CLI and seed do); test setup writes are attributed to a `FIXTURE_ACTOR` bound by the `db_session` fixture. Core bulk statements / raw SQL remain the caller's `record_event` responsibility. | Meaningful mutations must be auditable, and future writers (CLI commands, jobs, IProspect/IContact agents, Task 12 generic writes) will be added by other people: a forgotten binding must be a loud error in the first test, not a silent audit gap. |
| I-32 | 2026-09-10 | Task 06 | Settings API lives under **`/api/settings`**: `/settings/{roles\|activity-categories\|commercial-segments}` and `/settings/referents` (list with `q` + `active`, create, `PATCH` rename/activation, referent identity by `PUT`, delete). Business refusals answer a stable `detail.code` (`duplicate`, `in_use`, `invalid`, `not_found`) with structured data; the frontend owns the French copy. Services are `TaxonomyService` (`app/services/taxonomies.py`) and `ReferentService` (`app/services/referents.py`). Feature page: `doc/features/settings-taxonomies.md`. | One namespace for the Settings page and the pickers; codes instead of server-side French keep the API language-neutral like the rest (English `detail` messages) while giving the UI understandable, testable messages. |
| I-33 | 2026-09-10 | Task 06 | Taxonomy **ids and slugs are stable**: renaming changes the label only; the slug is generated once from the label at creation (`-2`, `-3`… when taken, `valeur` when empty) and never follows later renames. | References use the id; the seed (and later imports/agents) match on the slug — a slug that followed the label would make the seed re-insert a renamed suggestion and break any external key. |
| I-34 | 2026-09-10 | Task 06 | **Duplicates are compared case-, accent- and space-insensitively**, in the database (`label_key` expression indexes over `unaccent`, migration 0005, [ADR-0009](../adr/0009-settings-value-uniqueness.md)), inactive values included (reactivate instead of duplicating). Referents: full name unique on the same key (homonyms would be indistinguishable in pickers), e-mail optional but unique (lowercase). Amends ADR-0002's "label unique case-insensitively". | Task requirement; enforcing it in the database also covers Database Explorer edits (Task 12) and agents that bypass the services. |
| I-35 | 2026-09-10 | Task 06 | A value (or referent) is **deleted only when unused**; otherwise the API refuses with usage counts (prospects / companies / contact trackings) and the UI proposes deactivation. Deactivation hides the value from pickers but keeps existing references, and a picker still shows an inactive value while it is selected. Audit actions: `<taxonomy>.created/renamed/deactivated/reactivated/deleted` and `internal_referent.created/updated/deactivated/reactivated/deleted` (new `SettingsChange` vocabulary). | "Protect in-use values from destructive deletion", "deactivation does not orphan references" (task); meaningful action names make the history readable (Task 19). |
| I-36 | 2026-09-10 | Task 06 | **Inline creation from forms** (grill: taxonomies extensible from forms) is provided by reusable pickers `TaxonomySelect`, `TaxonomyMultiSelect`, `ReferentSelect` (`frontend/src/settings/selectors.tsx`, on a new accessible `Combobox` primitive): « Créer « … » » calls the same audited Settings API. Inline referent creation splits the typed text on its first space (first name / last name) and refuses a single word. The Settings page uses a **tab bar** of the four sections above one full-width panel instead of a left sub-nav. No optimistic UI. | Tasks 07/15 reuse one tested component; a single name field is enough for a quick inline creation and the referent can be completed in Paramètres. Tabs leave the tables room at 1280 px (a sub-nav left ~670 px); the server decides on duplicates/usage, so optimistic updates would show states it may refuse. |
| I-37 | 2026-09-10 | Task 07 | **Company editor entry points**: one wide drawer for the whole signed-in app (`CompanyEditorProvider` mounted by `AppShell`, opened with `useCompanyEditor()('new' \| id, { initialName, onSaved, onDeleted })`) and a compact **Entreprises** list at `/prospection/companies`, a secondary page of Prospection (the Prospection navigation item stays current); the Prospection placeholder links to it until Task 14, whose header should keep that link. No sixth navigation section. The Database Explorer *Ouvrir dans l'éditeur* action on `companies` rows is deferred until Task 12 (which edits the explorer's context actions in parallel) is merged. Saving keeps the drawer open on the saved company; Enter in a one-line field and Ctrl+S save; closing with unsaved changes asks first. | The spec lists five sections and places the Company editor under Prospect editor / global search / optionally Prospection; companies are prospection context, so a Prospection sub-page is the honest minimal home before Task 14. A provider-level editor lets Tasks 15/17 reuse it without routing changes. Keeping the drawer open after a save matches the Prospect editor's Save / Save & Next model. |
| I-38 | 2026-09-10 | Task 07 | **Company identifier and web rules** (`app/services/companies.py`): SIREN/SIRET ignore spaces and need 9/14 digits and a valid Luhn key (La Poste SIRETs, SIREN `356000000`: digit sum multiple of 5); a stored identifier left **unchanged** is not re-checked (the editor shows a warning); a SIRET not starting with the company's SIREN is a **warning**, not a refusal; uniqueness conflicts answer 409 `duplicate` naming the company that holds the value. `website_url` gets `https://` when typed without a scheme (http/https only); `email_domain` is stored lowercase without `@`, scheme, path or `www.`, and **webmail domains are refused** (the import's `WEBMAIL_DOMAINS`); the editor offers the website host as a domain suggestion, never fills it silently. 422 refusals carry `field` (a path such as `establishments.1.siret`) and a stable `reason`. | "SIREN/SIRET uniqueness errors are understandable. Company domain normalized" (task). Imported or explorer-edited legacy identifiers must not block unrelated edits; the SIREN/SIRET prefix can legitimately disagree while data is being corrected. A webmail domain names no employer and would make the import's company matching (same e-mail domain, I-59) attach unrelated people. |
| I-39 | 2026-09-10 | Task 07 | **Company save contract**: `POST /api/companies` creates a company with its establishments; `PUT` replaces the whole editable state — every field, the category list and the **full** establishment list (an omitted establishment is deleted; both lists are required in `PUT`). A non-empty establishment list has exactly one primary: none flagged → the first; two → 422; removing the primary hands it to the first remaining one (UI); the service writes removals and lost primaries in a first flush and the rest in a second so the partial unique index never sees two primaries, with one audit event per row. A company is deleted only when no prospect references it (409 `in_use` with the count); its establishments are deleted explicitly first, each audited. Created rows now record loaded many-to-many collections in their audit change set (`activity_categories_ids` on `company.created`). API refusal translation moved to `app/api/errors.py` (shared with Settings). | One atomic save per editor action (dirty bar); required lists prevent a partial client body from wiping establishments; the database index is not deferrable. Establishments carry no personal data, so auditing their deletion one by one keeps the company history complete (Task 19); categories were otherwise missing from a company's creation event. |
| I-40 | 2026-09-10 | Task 11 | Database Explorer exposure is **default deny**: `EXPOSED_TABLES` allowlists the 16 domain tables; every other ORM table must be listed in `UNEXPOSED_TABLES` with a reason (test-enforced); objects outside the ORM (`alembic_version`, catalogs) are unreachable; non-exposed = 404. Column policy `HIDDEN` / `MASKED` (none used yet; primary keys cannot be hidden). Authentication tables (Task 04) go to `UNEXPOSED_TABLES` on merge. | Credentials and session tokens must never be listable; a new table must be a conscious exposure decision. Task 12 extends the same `ColumnPolicy` with editability. [ADR-0005](../adr/0005-database-explorer-grid.md). |
| I-41 | 2026-09-10 | Task 11 | The explorer is a self-contained adapter in `app/services/explorer/` that builds its own SQLAlchemy Core statements (`statements.py`) from validated query objects, instead of going through `app/repositories`. Read API is **GET only**; the filter AST is a JSON query parameter. | It has no domain entities (generic tables), and repositories importing service-level query types would invert the layering. GET keeps it free of CSRF concerns and makes CSV exports plain links. |
| I-42 | 2026-09-10 | Task 11 | Filter semantics: `contains` / `starts_with` / global search are case-insensitive ILIKE with literal `%` / `_` (not accent-insensitive); text `eq` is exact; `neq` is `IS DISTINCT FROM` (keeps NULLs); JSON/array columns only support text `contains` and NULL checks; datetimes must carry a timezone offset. Row counts and totals are exact `count(*)`. | Predictable for a data explorer ("different from X" includes empty values); exact counts are cheap at V1 scale (50k-row test page < 2 s); estimates/keyset paging only if tables grow by orders of magnitude. |
| I-43 | 2026-09-10 | Task 11 | Explorer CSV export: current filter/search/sort, all rows streamed, UTF-8 **with BOM, `;` separator, CRLF**; text cells starting with `=`, `+`, `-`, `@`, tab or CR are prefixed with `'` except plain signed numbers (phone numbers such as `+33…` stay intact); hidden columns absent, masked columns empty. | French-locale Excel splits on `;` and needs the BOM for UTF-8; imported data is untrusted, so formula injection is neutralized. The business Excel export stays Task 10. |
| I-44 | 2026-09-10 | Task 11 | Page values longer than 240 characters (text, or JSON text form) are truncated and flagged; the record endpoint returns full values; a truncated preview is never offered as a filter value. | Keeps pages light while the value viewer shows everything. |
| I-45 | 2026-09-10 | Task 11 | Grid view state (search, sort, filters, page, page size) lives in the URL; criteria changes replace the history entry, FK hops push one carrying the origin (`Retour à …`). Column layout (order, visibility, widths, left pinning) is per table in `localStorage["viper.explorer.columns.<table>"]`; primary keys start pinned; UUIDs display as `xxxxxxxx…xxxxxxxx` in narrow columns. | Reload, deep links and browser Back work like a real tool; layout is a personal preference, not shareable state. UUIDv7 keys share their time prefix, so the tail is what distinguishes rows. |
| I-46 | 2026-09-10 | Task 11 | Grid built on headless **TanStack Table 8.21.3 + TanStack Virtual 3.14.11** (MIT) with our own markup and CSS (ADR-0005). New accessible `Menu` and `Popover` primitives in `src/ui/`. Ports are env-configurable (`VIPER_WEB_PORT`, `VIPER_API_TARGET`; Playwright: `VIPER_E2E_WEB_PORT` 5180, `VIPER_E2E_API_PORT` 8044, `VIPER_E2E_DATABASE_URL`, `VIPER_E2E_PYTHON`). One E2E infrastructure (I-25): the global setup also loads the synthetic explorer dataset (`python -m tests.e2e_data`, refuses non-`_e2e` databases) and every spec signs in through `e2e/session.ts`. | AG Grid Community lacks context menu/clipboard; TanStack Table 9 is one month old. Parallel worktrees need distinct ports and databases; one backend, one database and one login helper for all specs. |
| I-50 | 2026-09-10 | Task 08 | The import engine is a **pure function** `build_preview(file, reference, mapping)` in `app/services/imports/` ([ADR-0007](../adr/0007-import-engine.md)): no database, clock or randomness; reference data (roles, categories, segments, referents, companies, prospects + emails + contactability, inactive values included) arrives as a snapshot built by the read-only `reference_loader`. Legacy column names live only in `fields.py`. Output is a pydantic `ImportPreview` of proposals; unreadable files raise `ImportRejectedError`, everything else is a diagnostic with a fixed severity and a French message that never embeds cell values. | Task constraints (no DB side effects, deterministic, testable without the private workbook); Task 09 needs a JSON contract and Task 10 the legacy layout; values must stay out of logs. |
| I-51 | 2026-09-10 | Task 08 | Files: openpyxl **3.1.5** (+ `types-openpyxl` for mypy) in read-only `data_only` mode; merge ranges read from the sheet XML with the stdlib; a vertical merge's value is copied down its first column on rows that have content (flagged). CSV: UTF-8/BOM, UTF-16 BOM, else Windows-1252 (notice); delimiter sniffed among `;` `,` tab, tie → `;`. Encrypted `.xlsx` and `.xls` are recognised by their OLE signature. Limits (settings): 10 MiB, 5000 non-empty rows per sheet, 100 columns, unzipped XLSX ≤ 10 × the file limit. | Values, never formulas; bounded memory on untrusted uploads; French Excel exports use `;` and often Windows-1252. |
| I-52 | 2026-09-10 | Task 08 | Sheet recognition by header fingerprint: the sheet whose header row (first 10 non-empty rows) names the most known fields, ≥ 3 including an identity field; every other sheet gets `sheet.skipped`. Headers compared folded; the repeated `A contacter` is disambiguated by position (1st = planned-contact week, 2nd = legacy flag kept raw); other repeated/unknown/unnamed columns are kept raw with a notice. The user may override sheet, header row and column → field (Task 09). | The real sheet name has a trailing space and may be renamed; contract: extra sheets are skipped explicitly, unknown columns kept losslessly. |
| I-53 | 2026-09-10 | Task 08 | Legacy metadata format: `{key: {column, header, value, reason}}` with reason `opaque_field` (`Mode de contact`, 2nd `A contacter`), `unmapped_column` or `not_mapped_value`; raw is kept whenever a conversion is partial, impossible, a guess or not storable (e.g. week without year). Every non-empty cell is reported `mapped`/`preserved`; a final pass preserves anything unconsumed. A lone `0` in a text column is a placeholder (field empty, raw kept). | No silent loss (contract); `0` is used as "nothing" 246 times in `Type de projet` and as a civility anomaly. |
| I-54 | 2026-09-10 | Task 08 | Civility: `M`, `M.`, `MR`, `Monsieur` → `mr`; `Mme`, `MME.`, `Madame`, `Mlle` → `ms`; anything else invalid (raw kept). Names: collapsed spaces; only all-caps or all-lowercase names are re-cased (capitalised parts, inner particles `de/du/des/la/le/van/von/d'`… lowercase); mixed case kept. No name at all is an **error** (schema requires one); company missing is a warning. | I-11 left the variants to explicit import rules; re-casing must not destroy an author's deliberate casing or particles. |
| I-55 | 2026-09-10 | Task 08 | Planned contact: a week without year (`S37`) is a candidate **without date** (`requires_year`, raw kept) — never a guessed year; a week with its year → Monday of that ISO week; a date → that date. `retraité`/`décédé`/`plus en poste`… are not weeks and only *suggest* activity `inactive` (warning, user confirmation; import never sets activity). | Locked decision 15 / open question 3: never invent the year. |
| I-56 | 2026-09-10 | Task 08 | Legacy stages collapse into one status: `rdv obtenu` → `appointment_obtained`, `Devis envoyé` → `quote_sent`, `Suivi` → `quote_follow_up`, `Relance 1/2` → `follow_up_1/2`; the **most advanced positive stage wins** (enum order). Conflicts (`tracking.stage_conflict`, `requires_review`): a negative stage below the winner, `Relance 2` without `Relance 1`, `Suivi` without `Devis envoyé`; a quote without an appointment is not a conflict. A tracking proposal exists when a stage is positive, a planned contact was read or a referent was recognised (`to_contact` otherwise). | Locked decision 16; the task asks for an explicit precedence and visible conflicts. |
| I-57 | 2026-09-10 | Task 08 | Contact channels: emails split on spaces `;` `,` `/` `|`, lowercased, syntax-checked, first valid = primary; the company `email_domain` is the first non-webmail domain. Phones: French numbers → `+33…`; number-typed cells that lost their leading zero are restored (flagged); type by the French numbering plan, else by column; **primary = first mobile, else first number**; duplicates across `Téléphone`/`Mobile` dropped. | A direct mobile line is the most useful B2B channel; storage format is fixed by ADR-0002. |
| I-58 | 2026-09-10 | Task 08 | Matching: categories never split on `&`/`et` (historical labels contain them) but on `/ ; , + |` and line breaks, numeric prefix `N. ` removed, whole cell tried first; a part equal to a commercial-segment label becomes a segment suggestion. Roles: exact folded label/slug, else `contains`/close-spelling suggestions to confirm; an exact match on a deactivated value is reported, never duplicated. Referents: full name (either order) exact; unique first name, last name or initial + last name = partial (to confirm); markers, anything with `@` (even a referent's own address), week codes and notes never become referents. | Real categories look like `N. Label & Label`; contract: only genuine referents map, taxonomies are never created silently. |
| I-59 | 2026-09-10 | Task 08 | Dedup candidates, never decisions: same email (1.0), same names + company key (0.9), same names elsewhere (0.5); companies by key (legal forms/accents/punctuation ignored; 0.9, 1.0 if identical name), non-webmail domain (0.7), close spelling (0.6); in-file company variants and conflicting company-level values flagged. A row matching a `do_not_contact` prospect by email or names + company is **blocked** (error); a name-only match with one is a warning. Proposals never carry contactability, so Task 09 may only skip or attach to the blocked prospect. | Duplicate emails and company variants exist in the real file; import must never reactivate an opposition (security spec, I-12). |
| I-60 | 2026-09-10 | Task 12 | Database Explorer writes are **staged in the browser per table** and sent as one change set (`POST /api/explorer/tables/{table}/changes`: updates by key with the row version, inserts, deletes), validated against the explorer metadata and applied **all or nothing** through the ORM in the request's transaction (audited, `source=database_explorer`); refusals come back per change and per column in French (422, 409 on conflict). Leaving the table with staged changes asks for confirmation. [ADR-0008](../adr/0008-explorer-staged-writes.md). | The user asked for DBeaver-like editing with pending changes and Save/Cancel; the audit and the service rules only hold if writes go through the ORM in one unit of work. |
| I-61 | 2026-09-10 | Task 12 | **Optimistic concurrency per row with `updated_at`** (trigger-maintained): a save whose row version differs from the one the client read is refused with a conflict and nothing is applied. | `updated_at` exists on every writable row and moves on every UPDATE whatever the write path; comparing original field values would depend on exact round-trips of timestamps/text. Conservative (any concurrent change of the row conflicts) but safe. |
| I-62 | 2026-09-10 | Task 12 | **Editability policy** (default deny, `policy.py`): taxonomies, referents, companies, establishments, emails, phones, contact tracking editable (insert/update/delete); taxonomy `slug` and child rows' parent key set at creation only; `prospects` updatable and deletable but **not created** from the explorer (provenance is recorded by Prospection); opposition columns (`contactability_status`, `do_not_contact_at`, `do_not_contact_reason`) read-only; `prospect_sources` only `legal_basis_or_collection_context` and `notes` editable, no insert/delete; `import_batches`, `import_row_metadata`, `contact_tracking_status_history`, `audit_log` read-only; primary keys with defaults, timestamps, JSON/array values read-only everywhere. | Generic edits must not bypass durable opposition, provenance or import traces; child re-parenting and slug changes are rare and error-prone (delete and recreate instead); JSON editing is not safe enough for V1. |
| I-63 | 2026-09-10 | Task 12 | `company_activity_categories` (not audited as rows, Task 05) is **insertable/deletable** in the explorer, written through `Company.activity_categories` so each change is audited as `company.updated` with `activity_categories_ids`; updates are refused. A test forbids explorer writes to any other table of `NOT_AUDITED_TABLES`. | Keeps the audit complete without adding the link table to the audited registry; linking a company to a category is a legitimate data-explorer operation. |
| I-64 | 2026-09-10 | Task 12 | Explorer writes **delegate to the domain services** where a rule owns the column: `prospects.company_id` → `prospects.change_company` (I-13; cannot be emptied here), contact tracking → `save_contact_tracking` (status history; one row per prospect enforced before insert). | The company-change rule applies to any change of `company_id` (I-13) and the tracking history must not be bypassed; delegation avoids duplicating the rules. |
| I-65 | 2026-09-10 | Task 12 | Deleting from the explorer: a confirmation dialog shows server-computed diagnostics (RESTRICT blockers, CASCADE / SET NULL effects walked recursively, do-not-contact blocker); several rows at once only on tables from which no deletion cascades; rows removed by `ON DELETE CASCADE` are not audited one by one (the parent's deletion is), which the dialog states. | "Destructive actions are explicit"; bulk deletions must never silently take dependent rows; auditing each cascaded child would copy their personal values into the append-only log. |
| I-66 | 2026-09-10 | Task 12 | Backend French UI messages use the typographic apostrophe like the frontend: ruff `allowed-confusables = ["’"]`. Editor conventions: an emptied nullable field means NULL (explicit `NULL` button too); datetimes are edited in browser local time and sent as UTC ISO 8601; no silent normalization of values (CHECK messages explain the expected format). | Consistent copy; predictable, explicit values in a data explorer. |
| I-67 | 2026-09-10 | Task 13 | The read-only SQL console's security boundary is a **dedicated PostgreSQL login role** (`viper_sql_reader`: LOGIN only, no membership, `default_transaction_read_only`, statement/idle/lock timeouts, column-level `SELECT` on exactly the visible unmasked columns of the exposed tables, derived from the exposure policy and test-compared with it). Queries run on their own connection (NullPool), in a `READ ONLY` transaction with `SET LOCAL statement_timeout`, as a prepared statement through a server-side cursor (≤ `max_rows + 1` rows fetched); keyword/multi-statement checks only give readable messages. Every attempt is audited as `explorer.sql_executed` with the query's **SHA-256 and length, never its text**, plus outcome, row count, truncation and duration. [ADR-0011](../adr/0011-read-only-sql-console.md). | "Frontend checks are insufficient; backend/database role must prevent writes." A parser is only as good as its grammar coverage; privileges are PostgreSQL's own guarantee. The query text can contain personal literals and the audit log is append-only with undecided retention (I-27); the hash keeps verifiability. |
| I-68 | 2026-09-10 | Task 13 | The reader role is provisioned by an explicit, idempotent command, `python -m app.cli provision-sql-reader` (to re-run after every migration), not by a migration; without CREATEROLE it prints the SQL for an administrator. Password in `VIPER_SQL_READER_PASSWORD` (the default `viper_sql_reader` is a local-development value like `viper`/`viper`). pytest and the Playwright setup provision their databases automatically; the Docker init script is unchanged (the command creates the role with the local superuser). | Migrations never read application settings (ADR-0002) and may not hold CREATEROLE in production; grants need only table ownership, so the command keeps them in sync with the policy. |
| I-69 | 2026-09-10 | Task 13 | Console limits: one `SELECT` / `WITH` / `VALUES` / `TABLE` / `EXPLAIN` statement, 20 000 characters, 1 000 rows (`VIPER_SQL_MAX_ROWS`, « Résultat tronqué » beyond), 5 s (`VIPER_SQL_STATEMENT_TIMEOUT_MS`), 500 characters per cell; advisory locks, `set_config` and other session state are harmless because the connection is closed after each query; catalog object names stay readable (never rows of hidden tables). UI: a drawer opened from the page header, Ctrl+Entrée, no tabs/history/autocomplete. Dialog primitive fix: the layer caps a dialog to the viewport and its body shrinks and scrolls (`grid-template-rows: minmax(0, 1fr)`, `min-height: 0`), so long drawers keep their header and footer in view. | Bounded and discreet, as asked ("do not overbuild"); the schema is public in the repository, so catalog names reveal nothing new. |
| I-70 | 2026-09-10 | Task 07 | **Similar companies** (`companies.find_similar`, `GET /api/companies/similar`): existing companies with the same name key (the import's `company_key`: legal forms, case, accents, punctuation ignored), a close spelling (similarity ≥ 0.85, the import's threshold) or the same normalized e-mail domain; at most 5, a warning with *Ouvrir « … »* while a **new** company is typed — nothing is refused. | "Does this company already exist?" is needed by the editor, Task 09 (company candidates) and Task 15 (inline creation); reusing Task 08's normalization keeps one definition of "same company". |
| I-71 | 2026-09-10 | Task 07 | Shared UI changes: the dialog layer's grid row is bounded by the viewport (a tall Drawer/Modal now scrolls its body — previously it grew past the screen); `FieldFrame` gains a non-blocking `warning` (icon + `warning-fg` text, no `aria-invalid`); `SearchField` primitive extracted from the Settings toolbar; `BuildingIcon`; token contrast test adds `warning-fg`/`success-fg` on `surface-raised`. | Found while building the first tall drawer; warnings are needed for SIRET/SIREN consistency and by Task 15's "yellow" unverified values; one search box for list toolbars. |
| I-72 | 2026-09-10 | Task 09 | **Stateless import review** ([ADR-0012](../adr/0012-stateless-import-review.md)): the browser keeps the file and sends it with every analysis and with the commit; the server stores no workbook bytes. The commit re-runs the engine on a fresh reference snapshot and is refused with 409 when the file fingerprint (`file_changed`) or the SHA-256 digest of the preview JSON (`preview_outdated`) differs from the reviewed one. Re-importing a committed fingerprint needs `acknowledge_reimport` (warning, not a block). | No personal data outside the domain tables and no expiry job; a commit applies exactly what the user saw, including a do-not-contact status or a duplicate that appeared meanwhile. |
| I-73 | 2026-09-10 | Task 09 | **Corrections run through the engine**: per-cell corrections (`company_name`, `civility`, names, `job_title`, `email`, `phone`, `mobile`, `address`, `planned_contact`) replace the cell before normalization, matching, dedup and do-not-contact blocking; the original stays in legacy metadata (`<field>_original`, reason `corrected`). Everything else is a decision applied at commit: grouped by folded raw value (role, category token, referent, invalid civility, week number, company key) or per row (resolution, inactive confirmation). New engine codes `mapping.unknown_row`, `mapping.uncorrectable_field`. | A corrected e-mail or name must not escape duplicate/opposition detection; one grouped choice resolves every row with the same raw value (the real file has 117 distinct job titles for 198 rows). |
| I-74 | 2026-09-10 | Task 09 | **Conservative defaults**: only exact matches on active values are applied; suggestions, inactive matches, markers, notes and unknown values are left out (raw kept) until the user chooses; a week without year gets a date only from an explicit batch or per-week year; `retraité` sets `inactive` only when confirmed. Row defaults: a do-not-contact match is excluded; a duplicate of an existing prospect (same e-mail, or same names + company) is attached to the best candidate; a same-person row of the file is merged into the earliest one; other rows are created. A company is linked by default only to the single best existing company with the same name key. | Locked decisions 11 and 15 and the task (“no silent corrections”, “never invented”); attaching by default avoids silent duplicates while never overwriting anything (I-75). |
| I-75 | 2026-09-10 | Task 09 | **Merge rules** (attach or merge into another row): fill empty prospect fields only (civility, names, exact title, role), set a company only when the prospect has none (via `change_company`), add missing e-mails/phones as `imported`/`unverified` (an existing address/number, even inactive, is left untouched; a new one is primary only when none is), `inactive` only over `unknown`, create the tracking only when absent (otherwise fill empty dates/referent, never change the stage), no tracking for a do-not-contact prospect, never read or write contactability. Existing companies are only completed (`companies.complete_company`: empty domain/segment/texts, missing categories, an establishment only when none). Values not applied stay in the row's legacy metadata. | “Never overwrite verified data silently” and “never lose unknown data”; the do-not-contact trigger remains the last guard. |
| I-76 | 2026-09-10 | Task 09 | **Explicit creation of Settings values from the import**: a role or activity category the user chooses to create is created at commit time, inside the import's savepoint, through `TaxonomyService.create_value` with the **user** as actor (`source=ui`); a label that already exists is refused (409 `duplicate`). Referents are not created from the import (map to an existing one; create it in Paramètres and re-analyse). Pickers in the review therefore disable inline creation. | Creating from the picker immediately would change the reference data (and the digest) under the review and leave orphan values after a failed import; referents need a first and a last name the raw value (often one word) does not give. |
| I-77 | 2026-09-10 | Task 09 | **Transactional commit with a failure record**: all writes in one savepoint of the request's transaction (values to create → batch `pending` by the user → companies, prospects, tracking, one `excel_import` source and one `import_row_metadata` per imported row by the import actor → batch `committed`). Any exception rolls the savepoint back and records a `failed` batch in the same transaction (the route returns 500/422 `commit_failed` instead of raising, so it commits). Errors are logged by class and row only. Dates without time are stored at midnight Europe/Paris. | “Any error → full rollback” while keeping the attempt visible in the history; database messages can quote imported values. |
| I-78 | 2026-09-10 | Task 09 | **Batch provenance**: `import_batches` gains `legal_basis_or_collection_context` and `source_reference` (migration **0006**); the legal basis is required at commit (UI default « Fichier historique Circoe — prospection B2B », editable) and repeated on every prospect source of the batch; `sheet_names` holds the imported sheet. `GET /api/imports` shows metadata only. Upload bodies are parsed inside the route after the session/CSRF guard and a `Content-Length` bound (file limit + 2 MiB), with the new dependency `python-multipart==0.0.32`. | Provenance must say under which basis a file was imported; FastAPI would otherwise parse (and spool) an unauthenticated upload before any dependency. |
| I-79 | 2026-09-10 | Task 09 | **Import page** `/prospection/import` (Prospection sub-page, I-37 pattern): file → sheet confirmation → review (summary tiles and remark chips as filters, *À résoudre* grouped by raw value, row table with detail, correction, duplicates and preserved values) → confirmation dialog → result, with the import history on the same page. Entry points « Importer Excel » on the Prospection placeholder and the Entreprises header until Task 14. Warnings never block; errors block until corrected or excluded. Engine fix: structured addresses kept their street line (it was silently dropped). | The task's workflow; Task 14 owns the final Prospection header. |
| I-80 | 2026-09-10 | Integration of Tasks 07 and 12/13 | Playwright runs **one project, specs in any order**, on one shared E2E database: specs that write use their own invented rows, and no spec asserts a global row count another spec can change (known synthetic rows, filters such as `actor_id = tests.e2e_data`, or counts read from the API first). The `company-writes` project that forced `companies.spec.ts` to run last is removed. | Ordering hacks hide coupling and break as soon as a third spec writes; relative assertions keep every spec independent. Tightened by I-81: counts read first still raced. |
| I-81 | 2026-09-10 | E2E isolation fix (amends I-80) | **E2E tests own their data; never assert a global count.** Playwright runs `fullyParallel` (every test, not only every file, in parallel and in any order) with the default workers on the one shared E2E database. The synthetic dataset loaded by global setup is **read-only** for specs (an edit of one of its rows is only staged, then cancelled or refused). A test that writes creates its own rows — through the UI or the audited API (`frontend/e2e/data.ts`: `createCompany`, `createReferent`) — named with `uniqueSuffix()` (worker index + per-worker counter, unique within the run; also the body of a valid synthetic SIREN/SIRET; `import.spec.ts` draws a random hex suffix because the import compares company names by similarity), and looks at them through a filter or a search. Exact counts and orderings are asserted only on such owned rows or on synthetic subsets no test changes, pinned by a marker created rows cannot carry (the 36 synthetic companies' `societeN.example.com` e-mail domains — in the explorer filter and the SQL console, replacing Task 09's `siren LIKE '0%'` which covers only 27 of them — or the loader's audit events); a name filter alone is not enough (`Fret Import …` from the import spec contains « fret »); the count of a whole table is compared with the API answer the page displays (captured response), never with a number known in advance or read at another moment. Rows added by tests sort after the synthetic ones in the default primary-key order (UUIDv7), so first-page synthetic rows stay in place. | I-80's relative counts (read first, compared later) still raced with concurrent inserts (`36` vs `37`), and tests of one file depended on each other (the duplicate-SIREN test needed the company created by the previous test; fixed names collided under `--repeat-each`). Owned data and filtered subsets make every test independent without serial projects or fewer workers. |
| I-82 | 2026-09-10 | E2E isolation fix | Shared UI primitives. **Dialog**: focus stays inside the top open dialog. When the focused control gets disabled (the editor's *Enregistrer* while and after saving with the mouse) or is removed, the dialog takes the focus back (a `MutationObserver` on the dialog); an Esc pressed while focus is outside every dialog closes the top one (document listener, only for Esc not already handled). Nested dialogs still close one at a time and editors still guard unsaved changes in `onClose`. **Combobox**: the « Créer « … » » option appears only once the list has loaded. | Browsers move focus to `<body>` when the focused element becomes disabled or disappears, so the dialog's own key handler never saw Esc (drawer stuck open after a mouse save) and Tab left the modal. Fixed once in `ui/Dialog.tsx` for every Modal/Drawer rather than per editor. Under full E2E parallelism a picker typed into before its list loaded offered only *Créer*, so Enter created a near-duplicate category (`entreposage` beside *Entreposage et stockage*) — a user typing fast could do the same. |
| I-83 | 2026-09-11 | Task 10 | **Normalized export architecture** ([ADR-0013](../adr/0013-normalized-excel-export.md)): one specification module `app/services/exports/spec.py` (sheets, column order, French headers, value getters, cell kinds, widths) is the only place knowing the workbook layout; getters read a read-only projection of the domain (`projection.py`, SELECTs through `app/repositories/exports.py`), never the cached import rows. Changing the order or a header = editing that module; no migration. | Task: "exact final column ordering is centralized/configurable", "export from domain state, not cached import rows". Same adapter pattern as the import's `fields.py`. |
| I-84 | 2026-09-11 | Task 10 | **Default column order** of the `Prospects` sheet (resolves open question #6 for V1, still editable centrally): Référent · Date de contact prévue · Semaine · Entreprise · Segment commercial · Catégories d'activité · Civilité · Nom · Prénom · Rôle · Intitulé exact · Statut activité · Date de vérification · E-mail · Téléphone · Type de téléphone · Adresse · Code postal · Ville · SIREN · Site web · Domaine e-mail · the four Circoe context texts · Suivi de contact · Statut depuis le · Date de réponse · Date de rendez-vous · Ne pas contacter · Date d'opposition · Motif d'opposition · Origine · Collecté le · Base légale ou contexte de collecte · ID VIPER · ID entreprise. | Grill section C priorities (Referent first, then planning/company, classification, identity/role, then tracking/outcome), keeping the legacy A–C and J–V neighbourhoods the operator knows; the verification and opposition fields added since sit with identity and outcome; ids last because they key the other sheets. |
| I-85 | 2026-09-11 | Task 10 | **Export semantics**: `Référent` = the tracking's internal referent only (`Prénom Nom`); `Statut activité` and `Date de vérification` (employment) are separate columns; `Date de contact prévue` is the real date and `Semaine` its ISO week `S37 2026` (Europe/Paris, week-numbering year); one `Suivi de contact` label (+ `Statut depuis le` from the status history, response and appointment dates) replaces the five legacy booleans; `Ne pas contacter` Oui/Non with date and reason is distinct from `Non intéressé`; a prospect without tracking has a blank status; the main-sheet provenance columns show the oldest source. | Locked decisions 4, 5, 6, 15, 16 and the source's opposition ≠ non-interest requirement: the export must not recreate the legacy conflations. |
| I-86 | 2026-09-11 | Task 10 | **Alias shape** (resolves open question #7): the main sheet holds the primary e-mail and phone (compatibility columns, blank without a primary); every e-mail and phone record, primary or not, active or former, is one row of the `E-mails` / `Téléphones` sheets with primary/active flags, verification status and date, origin and source reference, keyed by `ID VIPER` (the prospect id, also on the main sheet). | Deterministic layout independent of the data (extra `E-mail 2…n` columns would change with the data and multiply per-alias metadata); nothing omitted. |
| I-87 | 2026-09-11 | Task 10 | **Seven sheets, lossless**: `Prospects`, `Entreprises` (every company, with or without prospects), `Établissements` (all; the primary address is also on the main sheet), `E-mails`, `Téléphones`, `Provenance` (every source), `Données d'origine` (one row per key of every `import_row_metadata.legacy_metadata`: file, import date, sheet, row, column, original header, key, raw value, reason). Rows sorted in Python (folded texts, id as last tie-breaker). | "Useful imported data not silently lost": a per-prospect sheet alone would drop companies without prospects, secondary establishments, later sources and per-row legacy values; one row per legacy value keeps its source context without data-dependent columns. |
| I-88 | 2026-09-11 | Task 10 | **Typed, formula-free, deterministic XLSX** (openpyxl write-only): real dates (Europe/Paris day; date-time only for a non-midnight appointment); phones, SIREN, SIRET, postal codes and ids as Text-formatted string cells; every text forced to a string cell (openpyxl would store `=…` as a formula, `#N/A` as an error) and a text matching the explorer CSV's formula rule — now shared in `app/core/spreadsheet.py` — also gets `quotePrefix` instead of the CSV's leading apostrophe, so the value stays exact; XML-illegal control characters dropped and texts cut at Excel's 32 767 characters. The generation date is an input that sets the document properties and every zip entry date: same data + same date = same bytes. | Task: "correct types/dates", "formula-free deterministic output". In XLSX an apostrophe would become part of the visible value; a typed string cell never evaluates and `quotePrefix` keeps it text when re-typed. Determinism makes exports comparable in tests. |
| I-89 | 2026-09-11 | Task 10 | **Delivery**: `GET /api/exports/workbook` (protected; GET: no CSRF, no domain write), whole database, no query parameter yet (Task 14 may add Prospection filters on the same path), generated in memory, `VIPER_export_YYYY-MM-DD.xlsx` (Paris date), `Cache-Control: no-store`, nothing stored; audited as the new semantic action `export.generated` (entity `excel_export`, the signed-in user, rows per sheet and size only). UI: reusable `ExportWorkbookButton` (progress « Export en cours… », error « L’export Excel a échoué. Réessayez. ») in the Entreprises and Import headers until Task 14 places it in Prospection. | A download of the whole personal-data base is a meaningful event to trace without copying any value into the append-only log; no server-side file means no retention question. |
| I-90 | 2026-09-11 | Task 14 | **One canonical segment module** ([ADR-0014](../adr/0014-canonical-prospect-segments.md)): `app/services/prospection/segments.py` defines the 16 Prospection segments as SQL predicates over one FROM (prospect + company + single tracking + primary e-mail); counters are one aggregate `count(*) FILTER (WHERE …)` query with the same criteria as the list, so a counter equals its list total by construction; list row states (`verification_state`, `email_state`, `due`) come from the same expressions. Home (Task 16) reuses them verbatim. Definitions: [prospection-kpis.md](../features/prospection-kpis.md). | Task constraint ("counters/list use the same source-of-truth semantics", "avoid N+1"); materialized flags cannot hold a "today", and re-deriving per screen drifts. |
| I-91 | 2026-09-11 | Task 14 | **Verification segments.** `never_verified` = `employment_verified_at IS NULL` (a company change clears it, so a moved prospect is back there: its current context was never verified). `needs_recheck` = employment verified **and** (an active channel `unverified` that keeps a `last_verified_at` — the trace I-13 leaves — **or**, only when `VIPER_VERIFICATION_STALE_DAYS = N` is set, verified before the start of the business day N days ago); disjoint from `never_verified`. The setting is **unset by default** and the page says no age threshold is configured. | Open question #9: no business age threshold is invented; the reset-channel trace is the only data-backed "was verified, no longer is" signal. Disjoint segments make the verification queue unambiguous. |
| I-92 | 2026-09-11 | Task 14 | **Contact segments.** *Actionable* = contactable **and** activity ≠ `inactive` (opposed or departed people are never to contact or follow up; they stay under Opposition / Inactifs). *Contacted* = stage past `to_contact` or a response/appointment date. `to_contact` = actionable and not contacted, **including people without any tracking** (the untouched base); `due` = `to_contact` with a planned contact before tomorrow (business day); `no_response` = actionable, stage `contacted`/`follow_up_1`/`follow_up_2`, no response nor appointment date; `responses` = response date, appointment date or stage `response_received`+ (incl. `not_interested`); `appointments` = appointment date or stage `appointment_obtained`/`quote_sent`/`quote_follow_up`/`won`. Invariant `appointments ⊆ responses ⊆ contacted`; outcome segments keep opposed people (history). | The task asks to decide whether "no tracking" is to contact: an imported person nobody has contacted is exactly what is left to do. Excluding `inactive` from actionable queues follows the activity meaning (no longer in post) without inventing policy; dates recorded against a lagging stage (explorer edits) still count. |
| I-93 | 2026-09-11 | Task 14 | **E-mail segments**: `email_missing` = no primary e-mail (only secondary or former addresses counts as missing); `email_unverified` = primary `unverified` **or** `unknown`; `email_invalid` = primary `invalid`. | Home spec lists missing / invalid / unverified primary e-mail; `unknown` is not known to be deliverable. |
| I-94 | 2026-09-11 | Task 14 | **Business day = Europe/Paris** for "due" and planned-contact ISO weeks; `BUSINESS_TIMEZONE` moves from `import_commit.py` to `app/core/business_time.py` (the import stores dates at midnight in that zone; the Excel export of Task 10 imports it from there too). The API returns `today` with the counters and `planned_contact_week` (`2026-W38`) per row. | A contact planned "today" must be due all day in France whatever the server's zone; one constant for import and Prospection. |
| I-95 | 2026-09-11 | Task 14 | **Criteria** shared by counters and list: `q` = every word in first/last name or company display/legal name (`label_key`: case, accents, spacing ignored) **or** in any of the person's e-mail addresses; a number-like query of ≥ 4 digits also matches phone numbers (a national leading 0 dropped, so `06 12…` finds `+33612…`). Role/title are not searched. Filters `role`, `referent`, `tracking_status` accept `none`; `import_batch` matches the prospect sources of that batch. Sorts: name, company, planned contact, verification, updated — each ending with the id. **No new index or migration** (20 000 prospects: counters ≈ 0.25 s, deep page ≈ 0.15 s). | Task scope (search person/company/email, phone added by the brief); `none` isolates unclassified/untracked people for cleaning; a total order keeps pages stable. |
| I-96 | 2026-09-11 | Task 14 | **Prospection page**: counters in three groups (*Base*, *Vérification*, *Suivi de contact*) of toggle cards (`aria-pressed`, check mark, stronger outline); search + *Filtres* disclosure (open when a filter is set) + *Trier par*; one card per person, 50 per page, three columns. **URL contract**: API parameter names, defaults omitted; criteria changes replace the history entry, opening a prospect pushes one; `prospectionHref()` for deep links (Home). *Entreprises* moves from the placeholder to the page header (I-37); the placeholder's `action` prop and CSS go away. | "Spacious, readable people list, not a spreadsheet" while keeping the first people visible at 1280×800; the explorer's URL-state rule (I-45) applied to Prospection. |
| I-97 | 2026-09-11 | Task 14 | **Open-editor contract for Task 15**: `?prospect=<id\|new>`, `ProspectEditorContext` `{ canCreate, Editor }` receiving `{ target, queue, onNavigate }`; `ProspectQueue.next(id)` re-reads the current page before the next one so people leaving the segment after a save are not skipped. Default implementation: open the person's row in the Database Explorer (replacing the `?prospect=` entry) and *+ Ajouter un prospect* `aria-disabled` with « Disponible avec l'éditeur de prospect ». | No fake editor (brief); the explorer row is a real, useful fallback; Save & Next needs a queue that survives rows leaving the filtered list. |
| I-98 | 2026-09-11 | Task 14 | *Exporter Excel* in the Prospection header is Task 10's `ExportWorkbookButton` (full-database export; no filtered export in V1). Shared changes: `.btn` rendered as a link is no longer underlined, `.btn[aria-disabled='true']` styling, `useCompanies(search, offset, limit)`, `renderApp(path, { wrap })` for extra providers, E2E helpers `syntheticWorkbook` / `importProspects` in `e2e/data.ts` (import.spec reuses the first). | Keeps the Task 10 merge trivial; a focusable disabled button keeps its tooltip; specs create their prospects through the real import API (I-81). |
| I-100 | 2026-09-11 | Task 15 | **One atomic Prospect editor save** ([ADR-0015](../adr/0015-prospect-editor-save.md)): `app/services/prospect_editor.py` composes the domain operations in the request's transaction — inline role creation (`taxonomies.create_value`), `prospects.change_company` (I-13), identity/employment fields, e-mail/phone full lists (`contact_channels.save_channels`), `save_contact_tracking`, and on creation `provenance.add_manual_source`. `GET/POST/PUT/DELETE /api/prospects` with the full view model (company summary, aliases with `imported_unverified`, tracking in business days, sources, `verification_state` from the Prospection expression). Unknown payload fields are refused (422). | Task: "Save is atomic", "no company field duplication"; services flush and the caller commits (I-17), so a refusal after a partial write rolls everything back (tested through the API). Refusing extras makes a client that believes it can set contactability fail loudly. |
| I-101 | 2026-09-11 | Task 15 | **Explicit verification actions.** Employment: `employment_verification.action` = `keep` / `verified_now` / `verified_on` (a business day at midnight Paris; today = now; future refused) / `clear`. Aliases: `verified_now` (dated now by the server); status `verified` without it only for an alias already verified and unchanged **after** the company-change rule — otherwise 422 `verification_action`. Other statuses keep `last_verified_at`. An edited address/number is a new value: origin `manual`, `last_verified_at` cleared. After a company change, `keep` leaves the employment cleared unless the same save verifies it. | Diffing a whole-state payload cannot tell "left as loaded" from "confirmed again" (a same-day re-verification would be lost, a naive client would undo I-13); an explicit action is unambiguous and auditable. |
| I-102 | 2026-09-11 | Task 15 | **Aggregate optimistic version**: `version` = SHA-256 (32 hex) of the `(kind, id, updated_at)` of the prospect, its e-mails, phones and tracking; every write (save, contactability, delete) locks the prospect row (`FOR UPDATE`) and compares it — stale → 409 `conflict`, the editor offers *Recharger la fiche*. The contactability operation returns the new version, which the editor adopts without touching the draft. | `prospects.updated_at` alone misses a concurrent alias or tracking edit (Database Explorer, another tab) that the full-list save would overwrite; the `set_updated_at` triggers make the fingerprint catch every write path, insertions and deletions included, with no new column. |
| I-103 | 2026-09-11 | Task 15 | **Alias lists**: normalized with the import's rules (e-mail syntax, French phones `+33…`); the same value twice refused (`repeated`); exactly one active primary (none flagged → first active; two → `multiple`; inactive primary → `inactive`); written in two flushes (deletions and lost primaries first) so the partial unique indexes never see two primaries; *Désactiver* keeps a former value, *Retirer* deletes the line; a value swap between two lines is refused with `exchange` (save in two steps). The phone type is preselected from the numbering plan in the UI and required by the API. | Mirrors I-39's establishment rules; unique indexes are not deferrable in PostgreSQL; former addresses matter after a company change (grill E). |
| I-104 | 2026-09-11 | Task 15 | **A role created from the Prospect editor is created with the save** (`role_label` in the payload; same `TaxonomyService` rules and `role.created` audit by the user; a duplicate → 409 shown on the role field), unlike the Company editor's pickers which create segments/categories immediately. Referents keep the immediate creation of `ReferentSelect` (first and last name, Paramètres rules). | Grill G: inline extensibility from the form — done atomically, so a cancelled or refused save leaves no orphan role; the picker says « sera ajouté aux rôles à l'enregistrement ». |
| I-105 | 2026-09-11 | Task 15 | **Company picker = server search** over every company (`GET /api/companies?q=`, 20 results) with « Créer l'entreprise « … » » opening the Company editor prefilled (its similar-companies check applies); the company it saves is selected. Shared primitives extended: `Combobox` gains `id`, `warning`, `onQueryChange` and a `create.run` that may resolve to `null` (cancelled); `CompanyEditorProvider` gains `onClosed`; `FieldFrame` sets `data-warning` (warning outline, text + glyph below). | A picker over the first 200 companies (the Prospection filter's) would miss existing companies and invite duplicates at creation; creation stays in the one Company editor (no duplicated company fields). |
| I-106 | 2026-09-11 | Task 15 | **Manual creation**: company required (UI and API: 422 `company_id` `blank`); provenance required — collection context / legal basis default « Saisie manuelle — prospection B2B » (editable), optional source reference; a new person's activity starts at *Inconnue*. *Enregistrer et nouveau* keeps the company and the provenance texts for the next person. | Source-of-truth provenance requirement; no legal basis is asserted by default, only the collection context; no activity is assumed; entering several people of one company is the common manual case. |
| I-107 | 2026-09-11 | Task 15 | **Tracking dates through the API are business days** (`planned_contact_on`, `response_received_on`, `appointment_on` + optional `appointment_time`), converted at midnight / that time in Europe/Paris by the server; a day equal to the stored moment's business day keeps the stored moment (no spurious event for an explorer-set time). `tracking: null` leaves the tracking as it is (never deleted from the editor). `business_time` gains `business_day` and `business_moment` beside Task 16's `start_of_day`. | No time-zone arithmetic in the browser; consistent with the import (I-77) and the week display (I-94). |
| I-108 | 2026-09-11 | Task 15 | **Opposition and deletion from the editor**: the opposition has its own operation, `PUT /api/prospects/{id}/contactability` `{do_not_contact, reason, version}`, a reason required both ways (the domain's `mark_do_not_contact` keeps its optional reason for other callers). Deletion is version-checked, refused (409 `do_not_contact`) for an opposed prospect with an explanation, otherwise one `prospect.deleted` event and database cascades (e-mails, phones, tracking and history, sources, import-row traces) that the confirmation dialog lists. | I-12 / ADR-0002 (clear first, then delete) and ADR-0008's cascade audit limit; a reasoned opposition is a compliance record. |
| I-109 | 2026-09-11 | Task 15 | **Editor UI**: a 64 rem drawer in two columns (person / context), the default of `ProspectEditorContext` (the explorer fallback is removed; Home now shows *Ajouter un prospect*); visual feedback always glyph + text (warning edge/outline/« Importé, à confirmer », success badge with date, stale only with the threshold — also for aliases —, company-change banner, e-mail domain mismatch warning); *Enregistrer et suivant* through `ProspectQueue.next` (queue order when opened: *Prospect 2 sur 3*), end of list said; Ctrl+S, Ctrl+Entrée, Échap (guarded), Enter in a one-line field saves like the Company editor; the opposition dialog's submit stops at the dialog (React events cross portals). | Interface spec and task; low effort at 1280–1440 px; one keyboard model across editors. |
| I-110 | 2026-09-11 | Task 16 | **Home reuses the Prospection segments verbatim**: `HomeService` (`app/services/home.py`) calls `count_segments` without criteria and builds its next actions from the same predicates (`predicate(DUE)`, `actionable`, `responded`, `has_appointment`); every prospect card links to `/prospection?segment=<key>` and equals that counter (tested by calling both services). Companies = every company row. Definitions: [home-dashboard.md](../features/home-dashboard.md). | Task 14 / ADR-0014: one source of semantics; a Home card must open a list of exactly the people it counts. |
| I-111 | 2026-09-11 | Task 16 | **Lightweight commercial counts** (*Devis envoyé*, *Suivi du devis*, *Gagné*, *Pas intéressé*) = prospects whose **current** tracking stage is that value, and nothing else (no date inferred, no cumulative "reached quote" figure); each links to `/prospection?tracking_status=<stage>`. | Task: "quote/follow-up/won only if manually recorded"; the current stage is the only recorded fact and maps exactly to an existing Prospection filter. |
| I-112 | 2026-09-11 | Task 16 | **Prospects newly contacted in month M** = the tracking's **first** transition into a contacted stage (`CONTACTED_STAGES`) in `contact_tracking_status_history` falls in M (Europe/Paris month), **unless that first transition was written by an import** (`actor_type = import`). Later moves never count again; opposed/inactive people still count. | The source's "100 contacts / month" measures new people reached. An import restates a legacy contact made at an unknown earlier date (I-15): counting it — or the follow-up after it — would inflate the month of every import. The status history is the only dated record of the transition. |
| I-113 | 2026-09-11 | Task 16 | **Appointments obtained in month M** = first transition into an appointment stage (`APPOINTMENT_STAGES`: `appointment_obtained`, `quote_sent`, `quote_follow_up`, `won`) in M, same import rule — **not** `appointment_at` in M. | The "10 rendez-vous / month" target measures meetings secured by the prospecting work; `appointment_at` is the meeting's own date (often next month, overwritten on reschedule) and would count meetings held. Every stage change writes history — Database Explorer tracking edits also go through `save_contact_tracking` (I-64) with the signed-in user — so they count; limit (documented): an appointment recorded only as a date, without a stage change, is in the `appointments` segment but not in the monthly figure. |
| I-114 | 2026-09-11 | Task 16 | **Informative targets as settings** `VIPER_MONTHLY_CONTACT_TARGET` (100) and `VIPER_MONTHLY_APPOINTMENT_TARGET` (10); the progress panel (current month + the 5 before as small columns, meter capped at the target with a text value, a table view) has the same weight as the recent activity panels, below the KPIs and next actions. | Interface spec: targets from the source requirement, "do not make 10 meetings the sole dominant UI metric"; grill A: global visibility first. |
| I-115 | 2026-09-11 | Task 16 | **Next actions** = three lists of 5 with totals, in this order: appointments from today to the 6th day after (actionable, soonest first), the `due` segment (oldest planned first), answers without appointment (actionable, `responded` and not `has_appointment`, stage ≠ `not_interested`, oldest answer first). Each person opens `/prospection?segment=<group segment>[&sort=planned_contact]&prospect=<id>` so Save & Next walks that queue. | "No invented business policy": fixed-time commitments, then overdue work, then conversions waiting; within a group only the dates already recorded order people. |
| I-116 | 2026-09-11 | Task 16 | **Recent activity**: the 5 latest import batches, and the latest human saves on `prospect` / `company` subjects — `audit.recent_activity` gains an `actor_types` filter; events of one request on one record form one line (8 lines from 40 events). The API returns structured actions only (action, entity type, tracking stage before/after, source, the record's current name) — never field values or raw `changes` / `context`; the French wording is one small formatter (`frontend/src/home/activity.ts`) that Task 19 will replace. | Imports have their own list (their per-row events would flood the feed); sign-ins, exports, SQL queries and Settings changes are not activity on the base. Task 19 owns readable history; Home must not expose raw JSON. |
| I-117 | 2026-09-11 | Task 16 | Every navigation section now has its page: the Accueil placeholder and `shell/PlaceholderPage.tsx` are removed (routes list the five pages explicitly) and the obsolete shell test "renders placeholder pages without data" goes with them; shell/theme/exploitation/user-menu tests stub `/api/home` through `src/test/homeApi.ts`. `start_of_day(day)` moves to `app/core/business_time.py` (segments and Home share it); `BATCH_STATUS` is exported from `ImportHistory.tsx` for Home's import lines. | No dead code; the removed test asserted the placeholder that Task 16 replaces (Home has its own tests). |
| I-120 | 2026-09-11 | Task 17 | **Global search contract**: `GET /api/search?q=` (session, read-only) answers `{query, groups: [{type, items, has_more}]}` — non-empty groups of `prospect` / `company` / `establishment` hits, **5 per group**, the group whose first hit matched best first (ties: prospects, companies, establishments). Every hit: `id`, `label`, `sublabel`, `match {field, kind, value}`, `badges` (`do_not_contact`, `inactive`, `primary`), `open {editor, id}`, `record {table, id}` plus typed context (prospect: company; company: SIREN, domain, primary city, prospect count; establishment: company, SIRET). `q` = 2–200 characters after collapsing spaces, else 422 `invalid` / `q` / `length`. Never raw rows. Definitions: [global-search.md](../features/global-search.md). | Task: "result contract exposes typed entity IDs/context, not raw DB rows"; five per kind keeps the popup compact at 1440×900 while a SIREN query can still bring its company first. |
| I-121 | 2026-09-11 | Task 17 | **What is searched**: prospects by their own first/last name (any word order), **any** e-mail address (former ones included) and phone number; companies by display/legal name, SIREN, e-mail domain and website host; establishments by SIRET, name and city. A 14-digit SIRET also finds its company; a one-word e-mail address or URL is also read as a host and finds the company of that domain. Prospects are **not** matched through their company's name (unlike Prospection's `q`, I-95); role, title, notes and Circoe texts are not searched. | Handoff scope (prospects by name/email/phone, companies by name/SIREN/site/domain, establishments optionally by SIRET; city added because it is cheap and distinguishes sites). A company query must list the company, not flood the Prospects group with its employees. |
| I-122 | 2026-09-11 | Task 17 | **Folding and the word rule**: texts compare through the new `search_key` (`unaccent` + lowercase — `label_key` without its whitespace regex); every word of the query must match the same field; a word of **≥ 3 characters matches anywhere**, a **shorter word only at a word start** (value start, or after a space, hyphen or apostrophe). Numbers: ≥ 3 digits for SIREN/SIRET, ≥ 4 for phones with the national leading 0 dropped (`prospection.query.phone_needle`, now public). `%`, `_`, `\` literal. | `label_key` costs ≈ 6.5 µs per value and 2-character substrings cannot use a trigram index: the first implementation had p95 232 ms (ADR-0017). Two letters typed are the start of a name (`ma` → Martin, not Thomas); the phone rule is Prospection's. |
| I-123 | 2026-09-11 | Task 17 | **Ranking**: `exact` (the whole value is the query: a name in any rotation of the query's words, an e-mail, a phone number in its `+33` forms, SIREN, SIRET, domain, website host, city) before `prefix` (every word starts a word of the value, or the value starts with the identifier) before `contains`; a row matched through several fields keeps its best match (then the first field of the list); ties by name (prospects last then first name; companies name; establishments company, primary first, name) and id. | "Exact/prefix before substring" (brief); a deterministic order for tests and keyboard use; the match field tells the UI which value to show (the e-mail or phone that matched). |
| I-124 | 2026-09-11 | Task 17 | **Indexes: `pg_trgm` GIN** (migration **0007**, [ADR-0017](../adr/0017-global-search-trigram-indexes.md)) on `person_search_key(first_name, last_name)`, `emails.address`, `phones.number`, `search_key(display_name)`, `search_key(legal_name)`, `search_key(establishments.name)`, `search_key(establishments.city)`; SIREN/SIRET/domain/website read by scans of the small company tables. Measured on 20 000 synthetic prospects: p95 39.8 ms (service), ≈ 55 ms (HTTP) vs 208.9 ms without index; B-tree `text_pattern_ops` rejected (starts-with only). **The hosting PostgreSQL must allow `pg_trgm`** (trusted since PG 13, like `unaccent`). | Task: decide `pg_trgm` vs B-tree prefix indexes with a 20k measurement, target p95 < 150 ms. Substring and word-start matching need trigrams; the write cost (≈ 10 µs per row) is negligible for V1. |
| I-125 | 2026-09-11 | Task 17 | **Opening a result**: a prospect pushes `/prospection?prospect=<id>` (the Task 14 open-editor contract, served by the Prospect editor of Task 15); a company opens `useCompanyEditor(id)`; an establishment opens its company's editor; **Shift+Enter** or the table button of a result opens its Database Explorer row (new `recordHref(table, id)` in `explorerView.ts`). Opening closes the popup and clears the field. | Reuse the editors that exist (no second editor, brief); the explorer row is the "Database context" of the handoff. Establishments have no editor of their own: they are edited in their company. |
| I-126 | 2026-09-11 | Task 17 | **Shell search UI**: header field (« Rechercher un prospect, une entreprise, un SIREN… ») with a WAI-ARIA combobox + listbox of named groups; **Ctrl+K / ⌘K** anywhere and **/** outside text fields, both ignored while a modal dialog is open; request 200 ms after the last keystroke; one TanStack Query entry per query with its AbortSignal, so a late answer never replaces the current one; the previous results stay dimmed and inert while a newer query runs (Enter acts only on current results); ↑/↓ wrap across groups, Enter opens, Esc closes then clears, Tab closes; hint below 2 characters, no-result, error with *Réessayer*, spinner; result count in a polite live region. New `MapPinIcon`. | Brief: debounced, cancel stale requests (last response wins), keyboard navigation, compact. Acting on dimmed results would open something the user no longer asked for; a focus jump out of an open editor would break its focus trap. |
| I-127 | 2026-09-11 | Task 17 | `app/services/search.py` builds its statements itself, read-only, like the Prospection query service (ADR-0014): one `UNION ALL` of per-field candidates per group, best match per row with `DISTINCT ON`, the folded key computed once per candidate (an `OFFSET 0` sub-select), 3 statements per search whatever the base size (tested). The router only shapes the typed hits. | The predicates are SQL by nature and serve this view only; repositories would add a layer without reuse. |
| I-128 | 2026-09-11 | Task 17 | **Performance test**: `tests/test_search_performance.py` seeds 20 000 synthetic prospects (2 000 companies, ~3 000 establishments, 25 000 e-mails, 20 000 phones) in the test transaction, merges the GIN pending lists (`gin_clean_pending_list`, what autovacuum does in a live base), warms up, then requires p95 < 150 ms over 5 × 28 searches through HTTP; with `CI` set: 2 000 prospects and 500 ms. | The handoff target measured where it matters (HTTP included) without making CI slow; shared runners are slower than the development machine, so CI checks for order-of-magnitude regressions (a scan or row multiplication) rather than the local target. |
| I-129 | 2026-09-11 | Task 17 | Supporting changes: `auth.spec.ts` asserts the signed-in user in the header (banner landmark, « Connecté : Pilote E2E ») — Home's activity feed also names that user once other specs edit data (flake found after Task 16); ORM trigram indexes declared through `models.common.trigram_index` (expression + `postgresql_ops`, so Alembic compares the expression; a changed expression is reported as drift); the `statements` test helper moves from `test_prospection.py` to `tests/builders.py` (shared by Prospection, Home and search tests). | Strict locators must target the element a test proves, not text that can legitimately appear twice; an opclass written inside the expression text would make Alembic skip the comparison. |
| I-130 | 2026-09-11 | Task 19 | **Readable history from one backend formatter** ([ADR-0018](../adr/0018-readable-history.md)): `app/services/history.py` turns audit events into typed display entries — actor `{kind: human\|import\|system\|agent, label, id, on_behalf_of}`, source, actions, a French title, value-free `summary` phrases and `changes` `{label, before, after}` as display strings (French field labels, enum wording, Europe/Paris dates, Oui/Non, references by the label snapshotted in the event or the current one, child rows by the address/number/name they had then). Every audited column of a prospect's or company's rows is labelled or listed in `NOT_SHOWN` with its reason (test). The French wording of this read-only display lives in the backend, unlike refusal codes (I-32). | The task asks for one formatter serving the editors and Home, with raw JSON kept out of screens; the interpretation of change sets (column names, label snapshots, policy markers) belongs next to them. |
| I-131 | 2026-09-11 | Task 19 | **One entry per save**: consecutive events with the same subject, actor, source and `request_id`; events without a request id (CLI, seed, jobs) group when consecutive with the same actor and source less than 5 s apart. Home's feed (events of all records, where concurrent saves interleave) joins a request's events on one record wherever they fall; one record's history keeps consecutive runs so its pages cut between entries. A primary flag moving between two e-mails (phones, establishments) of one save reads as one line « E-mail principal : A → B »; a first alias created primary reads « … · principal ». | One click on *Enregistrer* writes several row events (ADR-0006); a timeline of rows would read as noise and hide the meaning of a primary switch. |
| I-132 | 2026-09-11 | Task 19 | **History API** `GET /api/prospects/{id}/history` and `GET /api/companies/{id}/history` (session required): `limit` 1–50 entries (10), `before` = the previous page's `next_cursor` (the id of its last event); newest first by `occurred_at` then `id`; entries are never split between pages (events read 100 at a time until one more entry starts). The endpoints do not check that the record still exists: the audit outlives it. The Prospect editor shows 10 entries with *Voir plus*; the Company editor the same component. | Deterministic, stable paging while new events arrive (offsets drift); a history is still meaningful for a deleted record and costs no extra query. |
| I-133 | 2026-09-11 | Task 19 | **What is shown where**: the editors' *Historique* shows values **as stored** under the audit payload policy (full personal values in V1, I-27; « (masqué) » for masked markers; never a secret-looking or unlabelled field, never JSON). Home's *Dernières modifications* shows only the value-free `summary` (action phrases and contact-stage transitions) with the record's current name — no address, name change, reason or other company name. Home's feed now includes **agent** actors besides people (none in V1); imports keep their own list. | An editor is the operator's own record, already visible there; Home is a global screen seen at a glance, where values add risk and no decision. Agents' future saves are edits of the base the user must see. |
| I-134 | 2026-09-11 | Task 19 | **Actor and source display**: badge « Vous » (the signed-in user, by id), another person's name, « Import « fichier » » (+ « confirmé par … »), « Système » (+ the command's label: *Ligne de commande*, *Suggestions VIPER*), « Agent » (+ its label); sources *Interface*, *Import*, *Base de données*, *Ligne de commande*, *Agent*; relative (« il y a 5 minutes », up to a month) and absolute (« 11 sept. 2026 à 10:32 », Paris) dates. The import actor keeps its `Import <file>` snapshot; the formatter shows the file name (`IMPORT_ACTOR_PREFIX`). The provenance block uses the same badge: the view model's sources carry `recorded_by` (history actor) instead of `actor_display`. | Import and manual changes must be distinguishable at a glance; one actor model for history and provenance. |
| I-135 | 2026-09-11 | Task 19 | **Home API contract** (amends I-116): `recent_edits[]` = `{occurred_at, actor {kind, label, id, on_behalf_of}, source, subject_type, subject_id, subject_label, summary[]}` — `actor_display` and the structured `actions` (action, entity type, stage before/after) are gone; `frontend/src/home/activity.ts` keeps only the record name, the origin and counts/dates. The edit line wraps on two lines (full text as tooltip) instead of one truncated line. | I-116 announced that Task 19 replaces the minimal wording with the history formatter over the same data; the structured actions had no other reader. |
| I-136 | 2026-09-11 | Task 19 | Prospect editor: an **imported field left empty** (Rôle, Intitulé exact, Entreprise) shows an actionable empty state — « Aucun rôle — choisissez-en un ou créez-le. », « Aucun intitulé — saisissez le libellé de poste de la personne. », « Aucune entreprise — choisissez-la ou créez-la. » — instead of « Importé, à confirmer »; filled imported fields keep the warning. | Review of Task 15 (orchestration log): there is nothing imported to confirm in an empty field; the next step is to fill it. |
| I-137 | 2026-09-11 | Task 19 | Fix: the Prospect editor sends a stored alias's `source_reference` back **exactly as stored** (it is not editable there). It collapsed inner spaces, so every save of an imported person rewrote the import reference (the real sheet name ends with a space: `… / Base client  / ligne 2`) — a silent data change and a spurious « E-mail modifié » in the history. | Found by the history E2E: a save must write only what the user changed. |
| I-140 | 2026-09-11 | Home performance fix | **Whole-base statements are planned without nested loops or JIT** ([ADR-0019](../adr/0019-whole-base-statement-plans.md)): the segment counters (`count_segments`, so Home's cards too), the Prospection page and its total, and Home's three next-action groups run inside `app.db.session.whole_base_plan`. It sets `enable_nestloop` and `jit` to `off` with transaction-local `set_config` and resets them after the block. No index, no migration. Root cause of the 250–310 s Home test: an autovacuum that ran during the uncommitted seed recorded `reltuples = 0` on full tables. The planner then estimated 1 row per table and chose nested loops that scan the inner index once per outer row (counters 123 s, *Échus* 87 s, *Réponses* 186 s under `EXPLAIN ANALYZE`). Migration 0007 is not involved. | These statements read every prospect: a hash join is always the right plan and bounds them to linear time whatever the statistics say. The zero-row state is reachable in production when a VACUUM races a bulk import, until autoanalyze runs. JIT compilation cost 45–410 ms per call for a ≈ 55 ms statement. Alternatives (ANALYZE in the test, LATERAL lookups, ANALYZE after imports, database-level settings) are in the ADR. |
| I-141 | 2026-09-11 | Home performance fix | `tests/test_prospection_performance.py` measures the counters, a deep page and Home in **three planner states**, each within the 2 s budget: without column statistics (right after the seed), after a `VACUUM` run from a second connection while the seed is uncommitted (the tables look empty to the planner), and after `ANALYZE`. The base keeps 20 000 prospects on CI (no `CI` switch). The seed builds the prospects' companies with one join instead of a subquery per row, and Home's seed adds ≈ 52 000 import audit events newer than 1 % of prospects with human saves. | Measuring only after `ANALYZE` (what autoanalyze does in production) would hide the state that broke Home, and measuring only the unanalyzed seed made the result depend on autovacuum's timing. A 2 000-row base would put the quadratic plans at ≈ 2.5 s, too close to the budget to be caught reliably. |
| I-150 | 2026-09-11 | Task 20 | **Refresh after a write = cancel, then invalidate** (`frontend/src/api/refresh.ts`, `refreshAfterWrite(queryClient, keys)`), used by every write of the app (companies, prospects, Settings values and referents, import commit, Database Explorer change sets and refresh); an ESLint `no-restricted-properties` rule forbids a bare `invalidateQueries` outside that module. Audit of the list/search pages (Entreprises, Prospection, Paramètres, Base de données, import history): every key already carries its search/filter/page parameters and no page keeps fetched data in its own state (the import analysis keeps its own "latest request wins" guard; the SQL console runs one query at a time). | Found by the E2E-isolation work: TanStack Query cancels a request in flight on invalidation only when its query already holds data; the first request of a new key (a search typed just before a save) is reused, so its pre-save answer became the list's current data (the list showed the old primary city). A test with a held search request fails on the old code. |
| I-151 | 2026-09-11 | Task 20 | **SQL reader provisioning is serialized cluster-wide** (`sql_reader.provisioning_lock(url)`): every run (`provision-sql-reader`, the pytest fixtures that commit a provisioning, hence the E2E setup) holds a session advisory lock (key `0x56495052`) on its own autocommit connection to the cluster's `postgres` maintenance database from before its transaction until after its commit; a role that may not connect there locks in the target database instead. | The role, its password and settings are rows of catalogs shared by the cluster: two checkouts provisioning at once (different databases) failed with "tuple concurrently updated". Advisory locks are scoped to one database (verified: the same key is granted in two databases at once), so a lock in the target database would not serialize checkouts; the maintenance database is the one every cluster has. The lock must outlive the commit, so it wraps the caller's transaction rather than living inside `provision_sql_reader` (services never own the transaction, I-17). A test running two provisioning commands at once fails on the old code with exactly that error. |
| I-152 | 2026-09-11 | Task 20 | **No raised Vitest timeout**: the 15 s suite timeouts of `CompanyEditor.test.tsx` (Task 10 stop-gap) and `TableEditing.test.tsx` are removed. The two long Company editor flows are split into one behaviour per test (name required · normalized payload · add establishments · SIRET warning/repeat · one primary kept · establishments payload), *new prospect* into three, and long values are entered with the shared `fill` helper (`frontend/src/test/fill.ts`: clear + one paste). Slowest test: 5.7 s → 1.8 s, also with the backend suite running at the same time. | A timed-out test keeps typing into the next test's DOM, and a raised limit hides slow tests until they fail under load. Key-by-key typing is kept only where keystrokes are the behaviour tested; the assertions are unchanged. |
| I-153 | 2026-09-11 | Task 20 | `e2e/history.spec.ts` checks Home's feed on **the Home answer read right after its save** (`page.request.get('/api/home')` as soon as the PUT answers): the save must be exactly one entry there, then the Home page renders that captured, real answer (`page.route` replay) for the line and screenshot assertions. | Home keeps the 8 latest grouped saves of the whole shared base; with specs running in parallel, other saves between the save and the page load pushed the line out (reproduced by 9 saves in between: the old check fails, the new one passes). The check still proves the value — one save = one Home entry even when concurrent saves interleave its events (the Task 19 bug), rendered value-free with the actor — which the prospect's own history alone would not. |
| I-154 | 2026-09-11 | Task 20 | **Database Explorer header row = one tab stop** (WAI-ARIA grid pattern): a roving focus over the headers' sort buttons, following the grid's active column; ← / → / Home / End between headers, ↓ into the rows and ↑ from the first row back; the header's actions are keys — Enter sort, Shift+Enter add to the sort, Alt+↓ / Shift+F10 / context-menu key the column menu (sort, *Filtrer…*, pin, move, width, hide), Shift+← / → resize ±16 px with a spoken width; one visually hidden hint describes them. Filter and menu icon buttons leave the tab order (`tabIndex=-1`) and the resize edge becomes a mouse-only handle (`aria-hidden`); mouse behaviour is unchanged. | Each header carried four tab stops (sort, filter, menu, focusable resize separator): 20 columns meant 80 Tab presses before the rows. Every action of the removed stops stays reachable from the focused header, and the column menu already held the filter. |
